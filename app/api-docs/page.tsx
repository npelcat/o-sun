import SwaggerClient from "@/src/pageComponents/SwaggerClient";

export const dynamic = "force-dynamic";

export default function ApiDocsPage() {
  return (
    <div>
      <SwaggerClient />
    </div>
  );
}
